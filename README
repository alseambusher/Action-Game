note that this project is still under construction
also note that this project implements everything from basic level
things required:opengl libraries,g++,xdpyinfo utility
platform: c++ in LINUX
implementation on windows not yet released
just type ./BUILD_RUN to build and run
HOW TO INSTALL
sudo apt-get install libalut-dev libopenal-dev
then
./BUILD_RUN
